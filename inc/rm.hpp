#pragma once

#include "util.hpp"
#include <cmath>
#include <fstream>
#include <iostream>
#include <queue>
#include <vector>

bool rm(std::vector<task> tasks);
bool isRMSchedulable(std::vector<task> tasks);